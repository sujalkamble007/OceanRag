[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oceanrag"
version = "0.1.0"
description = "Deep-Sea Governance Research Assistant — RAG pipeline for ocean governance PDFs"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
